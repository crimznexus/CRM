# Server Configuration
PORT=5000
NODE_ENV=development
# Force SQLite for now - comment/remove this line to switch back to MySQL (PC2)
DB_DIALECT=sqlite


# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Database Configuration (local MySQL instance)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root123
DB_NAME=crm_db

# JWT Authentication
JWT_SECRET=your_actual_strong_jwt_secret_key
JWT_EXPIRES_IN=7d
JWT_EXPIRES_IN_REMEMBER_ME=30d

# Email Settings (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=alishba.bano188@gmail.com
SMTP_PASSWORD=ermb vals qbex azws
EMAIL_FROM="WESSMAA CRM <alishba.bano188@gmail.com>"

# Google Maps (Lead Discovery) - server-side only, NEVER put this in the
# frontend code or .env. Restrict this key in Google Cloud Console to
# only the Places API for safety.
GOOGLE_MAPS_API_KEY=AIzaSyAOGan2mgLxYrl9Y4NM7joS7deuqn9csfA