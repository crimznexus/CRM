// Root Vercel Function entry point. The implementation remains in the API
// workspace so local development and deployment use the same application.
module.exports = require("../apps/api/api/index");
